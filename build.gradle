plugins {
    id "java"
    id "application"
}

repositories {
    mavenCentral()
}

mainClassName = "com.teste.kafka.TesteKafka"

dependencies {
    implementation group: 'org.apache.kafka', name: 'kafka-clients', version: '2.8.0'
}
